<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jellyfishmix.wxinterchange.dao.TeamInfoDao">

    <resultMap type="com.jellyfishmix.wxinterchange.entity.TeamInfo" id="TeamInfoMap">
        <result property="tid" column="tid" jdbcType="VARCHAR"/>
        <result property="gid" column="gid" jdbcType="VARCHAR"/>
        <result property="teamName" column="team_name" jdbcType="VARCHAR"/>
        <result property="avatarUrl" column="avatar_url" jdbcType="VARCHAR"/>
        <result property="grade" column="grade" jdbcType="INTEGER"/>
        <result property="numberCounts" column="number_counts" jdbcType="INTEGER"/>
        <result property="createdNumberCounts" column="created_number_counts" jdbcType="INTEGER"/>
        <result property="managedNumberCounts" column="managed_number_counts" jdbcType="INTEGER"/>
        <result property="joinedNumberCounts" column="joined_number_counts" jdbcType="INTEGER"/>
        <result property="fileCounts" column="file_counts" jdbcType="INTEGER"/>
        <result property="creationTime" column="creation_time" jdbcType="TIMESTAMP"/>
        <!--无特殊情况不开放modifiedTime-->
        <!--<result property="modifiedTime" column="modified_time" jdbcType="TIMESTAMP" />-->
    </resultMap>

    <!--查询单个-->
    <select id="queryByTid" resultMap="TeamInfoMap">
        select
          tid, gid, team_name, avatar_url, grade, number_counts, created_number_counts, managed_number_counts, joined_number_counts, file_counts, creation_time
        from wx_interchange.team_info
        where tid = #{tid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TeamInfoMap">
        select
          tid, gid, team_name, avatar_url, grade, number_counts, created_number_counts, managed_number_counts, joined_number_counts, file_counts, creation_time
        from wx_interchange.team_info
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="TeamInfoMap">
        select
          tid, gid, team_name, avatar_url, grade, number_counts, created_number_counts, managed_number_counts, joined_number_counts, file_counts, creation_time
        from wx_interchange.team_info
        <where>
            <if test="tid != null and tid != ''">
                and tid = #{tid}
            </if>
            <if test="gid != null and gid != ''">
                and gid = #{gid}
            </if>
            <if test="teamName != null and teamName != ''">
                and team_name = #{teamName}
            </if>
            <if test="avatarUrl != null and avatarUrl != ''">
                and avatar_url = #{avatarUrl}
            </if>
            <if test="grade != null">
                and grade = #{grade}
            </if>
            <if test="numberCounts != null">
                and number_counts = #{numberCounts}
            </if>
            <if test="createdNumberCounts != null">
                and created_number_counts = #{createdNumberCounts}
            </if>
            <if test="managedNumberCounts != null">
                and managed_number_counts = #{managedNumberCounts}
            </if>
            <if test="joinedNumberCounts != null">
                and joined_number_counts = #{joinedNumberCounts}
            </if>
            <if test="fileCounts != null">
                and file_counts = #{fileCounts}
            </if>
            <if test="creationTime != null">
                and creation_time = #{creationTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id">
        insert into wx_interchange.team_info(tid, gid, team_name, avatar_url, grade)
        values (#{tid}, #{gid}, #{teamName}, #{avatarUrl}, #{grade})
    </insert>

    <!--通过tid修改数据-->
    <update id="update">
        update wx_interchange.team_info
        <set>
            <!--<if test="tid != null and tid != ''">-->
            <!--    tid = #{tid},-->
            <!--</if>-->
            <if test="gid != null and gid != ''">
                gid = #{gid},
            </if>
            <if test="teamName != null and teamName != ''">
                team_name = #{teamName},
            </if>
            <if test="avatarUrl != null and avatarUrl != ''">
                avatar_url = #{avatarUrl},
            </if>
            <if test="grade != null">
                grade = #{grade},
            </if>
            <if test="numberCounts != null">
                number_counts = #{numberCounts},
            </if>
            <if test="createdNumberCounts != null">
                created_number_counts = #{createdNumberCounts},
            </if>
            <if test="managedNumberCounts != null">
                managed_number_counts = #{managedNumberCounts},
            </if>
            <if test="joinedNumberCounts != null">
                joined_number_counts = #{joinedNumberCounts},
            </if>
            <if test="fileCounts != null">
                file_counts = #{fileCounts},
            </if>
        </set>
        where tid = #{tid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteByTid">
        delete from wx_interchange.team_info where tid = #{tid}
    </delete>

</mapper>